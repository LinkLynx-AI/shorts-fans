NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# Required only when server-rendered frontend code should call same-origin APIs through an internal endpoint.
# AWS dev sets this in Terraform. Local development normally does not need it.
# API_BASE_URL_INTERNAL=http://localhost:8080
