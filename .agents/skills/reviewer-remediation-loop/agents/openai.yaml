interface:
  display_name: "Reviewer Remediation Loop"
  short_description: "Repeat reviewer gates until blocking findings are gone"
  default_prompt: "Use $reviewer-remediation-loop to enforce the reviewer gate and keep fixing until the gate passes."
