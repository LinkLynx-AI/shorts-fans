interface:
  display_name: "Linear Implementation Simple Review Leaf"
  short_description: "Orchestrate leaf delivery with reviewer_simple loop"
  default_prompt: "Use $linear-implementation-simple-review-leaf to orchestrate a leaf issue with reviewer_simple until the gate is clean."
