interface:
  display_name: "Linear Implementation Leaf"
  short_description: "Orchestrate leaf delivery through repo implementation skills"
  default_prompt: "Use $linear-implementation-leaf to orchestrate a leaf issue from spec freeze through reviewer loop and PR handoff."
