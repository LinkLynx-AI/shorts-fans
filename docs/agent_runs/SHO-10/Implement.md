# Implement.md (Runbook)

- `Plan.md` を単一の実行順として扱う。順序変更が必要なら `Documentation.md` に理由を残す。
- diff は `SHO-10` の docs 作業だけに限定する。
- 各 milestone の完了前に `rg` と `git diff` で確認する。
- `Documentation.md` には判断、validation 結果、branch 作成制約のような blocked automation を継続記録する。
